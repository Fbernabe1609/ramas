using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ScriptSam : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //Esto es el start   
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
